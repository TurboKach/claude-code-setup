# codex challenge — range 83c9b33cbfcc0871e5558a652ef6df55c72e1223..ac1424d766bcdf74bd48d786a2b673fd7ae8c46d — checkout /Users/turbokach/Dev/claude-code-setup — exit 0 — 714s
- skills/feature-workflow/scripts/codex-challenge.sh:61 — An existing `--out` symlink is followed by shell redirection, so a malicious repo can make the conventional verdict path target any user-writable file and have the review truncate it.
- skills/feature-workflow/SKILL.md:18 — The final gate forbids `--pin` without requiring a clean tree, while repo-backed output creates growing `.log`/`.msg` files during the run; unstaged fixes or those sidecars can alter filesystem inspection and produce a false-clean verdict for committed `HEAD`.
- global/CLAUDE.md:27 — The gate hard-codes `~/.claude/...` even though install.sh:75 supports `CLAUDE_HOME`; custom-destination installs invoke a missing or stale script and can never satisfy the gate.
- skills/feature-workflow/scripts/codex-challenge.sh:18 — The default macOS/BSD `mktemp` template puts `.md` after `XXXXXX`, but BSD `mkstemp` requires trailing Xs, so the advertised invocation without `--out` aborts before review.
- skills/feature-workflow/scripts/codex-challenge.sh:14 — `--out` without an operand crashes with an unbound-variable exit 1, while `--out --pin` consumes `--pin` as the filename and silently runs unpinned against a concurrently changing checkout.
- skills/feature-workflow/scripts/codex-challenge.sh:12 — Validation accepts multiple separators such as `A..B..C`, then lines 16–17 silently discard `B` and review `A..C`, allowing a malformed slice specification to certify the wrong range.
- skills/feature-workflow/scripts/codex-challenge.sh:27 — With `TMPDIR` unset or set to `/tmp`, default umask creates the scratch hierarchy and detached worktree world-readable, exposing the complete repository to other local users during every pinned review.
- skills/feature-workflow/scripts/codex-challenge.sh:54 — Every attempt starts a persistent Codex session without `--ephemeral`; retries and review rounds retain redundant code/tool transcripts outside the wrapper’s cleanup and can exhaust disk independently of its worktree pruning.
- skills/feature-workflow/scripts/codex-challenge.sh:26 — `--pin` unconditionally requires undocumented `shasum`; minimal Linux environments that otherwise satisfy the advertised `git`, `codex`, and GNU `timeout` dependencies fail before creating the review checkout.